# Context

### `GET /api/v1/context`

Returns the context entry point agents use to orient themselves before acting. The intended surface is company, product, or task context with real-time state and guidelines; the current implementation is still a placeholder health-style response.

**Response `200`:**
```json
{"status":"ok"}
```

---